{
  "name": "science-worksheet-library",
  "version": "1.0.0",
  "description": "Resumable Science Worksheet Library — standalone JSON→PDF/PNG pipeline",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "generate": "tsx src/generate.ts",
    "generate:all": "tsx src/generate.ts --all"
  },
  "dependencies": {
    "playwright": "^1.52.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
