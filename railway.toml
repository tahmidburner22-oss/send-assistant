[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --no-frozen-lockfile && VITE_COVERAGE_MAP_ENABLED=1 pnpm build"

[deploy]
startCommand = "node dist/server/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
