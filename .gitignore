node_modules/
data/
# But allow the bundled curriculum spec-point taxonomies (FEAT-PC4) — they
# are static JSON committed alongside the code.
!client/src/data/
!client/src/data/**

# W2 (FEAT-H7) — production corpora are committed seed data, not runtime output.
!server/data/
!server/data/corpora/
!server/data/corpora/**
# Runtime files (e.g. feature-flag-allowlist.json from W3) stay ignored.
server/data/*.json
server/data/*.log
.env
*.db
*.db-shm
*.db-wal
dist/
.DS_Store
.env.local



# Worksheet scrutiny verification script artefacts
.verify-tmp/

# Build / debug noise
tsc_errors*.txt
wget-log
test_output.txt
test-render.mjs
*.log


# Worksheet eval harness — generated report (closes audit item #44)
server/tests/worksheet-eval/eval-report.json


# Maths gold-layout verification screenshots (generated by scripts/verify-maths-gold.mts)
output/

# Generated worksheet visual-inspection artefacts
audit/visual-pdfs/
audit/visual-pngs/
audit/visual-samples/
audit/current-example-pdfs/
audit/current-example-pngs/

# Transient local diagnostic output; the Markdown repair register remains tracked.
audit/typecheck_*.txt
