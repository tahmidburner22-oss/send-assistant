[phases.setup]
nixPkgs = ["nodejs_22", "poppler_utils"]

[phases.install]
cmds = [
  "npm install -g pnpm@10.32.1 && pnpm install --no-frozen-lockfile"
]

[phases.build]
cmds = ["npm install -g pnpm@10.32.1 && pnpm build"]

[start]
cmd = "node dist/server/index.js"
